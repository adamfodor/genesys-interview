spring:
  application:
    name: genesys-interview
  datasource:
    url: jdbc:postgresql://localhost:5432/usermanagement
    username: admin
    password: admin

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html

app:
  jwt:
    secret: my-secret-key-my-secret-key-12345679841354687